# Typerscript Project

Este é um projeto Node.js + TypeScript que utiliza Express para executar um servidor backend em desenvolvimento usando `ts-node-dev`.

## 🚀 Tecnologias utilizadas
- **TypeScript**
- **Node.js**
- **Express**
- **ts-node-dev**

## 📂 Estrutura básica
- `Backend/server.ts` – ponto de entrada do servidor
- `package.json` – scripts e dependências
- `node_modules/` – dependências do projeto

## ▶️ Como executar
1. Instale as dependências:
   ```bash
   npm install
   ```
2. Inicie o servidor em modo desenvolvimento:
   ```bash
   npm run dev
   ```

## 📦 Scripts disponíveis
- `npm run dev` — inicia o servidor com recarregamento automático.

## 📝 Sobre
Projeto criado como prática de TypeScript com servidor Express. Flexível para APIs, microserviços ou estudos da linguagem.
